import {
    Container
} from './styles'

const AdmSidebar = ({adm}) => {
    return (
        <Container>
            <p>{adm}</p>
            <p>{adm}</p>
        </Container>
    )
}

export default AdmSidebar